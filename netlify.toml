[build]
  base = "."
  command = "npm ci --prefix frontend ; npm run compile ; npm run build --prefix frontend"
  publish = "frontend/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
